<%= form_for(indication) do |f| %>
  <% if indication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(indication.errors.count, "error") %> prohibited this indication from being saved:</h2>

      <ul>
      <% indication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :indication_name %>
    <%= f.text_field :indication_name %>
  </div>

  <div class="field">
    <%= f.label :is_active %>
    <%= f.check_box :is_active %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
